// Code generated by raf using Material interface.
// - Namespace: 01987547fc6c72ecb453bd2736bd4ea0.
// - Entity Name: diagnostic_result.
// - Entity ID: 102019a692c38da79208e71c127a9db9e84.
// - Entity Version: 102019a6e88bed67236939f705e5b6af790.
// - Task ID: 69120bf9228d86cc3a65654e.
// - Task Root ID: 6910a741b1b70bd284b566f2.

import { DataType } from "./common";
import type { Value, Page, Index, Filter, Sort, Data } from "./common";
import { DataStoreClient, CreateData, CreateValue, ParseValue } from "./client";







/**
 * Enumeration for DiagnosticResultUrgencyLevel
 */
export enum DiagnosticResultUrgencyLevel {
  Unspecified = 0,
  Low = 1,
  Medium = 2,
  High = 3,
}



/**
 * Interface for DiagnosticResultModel
 * `id`, `data_creator`, `data_updater`, `create_time`, `update_time` should not be set when INSERTING data since backend will fill it automatically.
 * `id` should keep the original value when setting (updating) data.
 * `data_creator` and `data_updater` are read-only strings (user id) representing the creator and updater of the data record.
 * `create_time` and `update_time` are read-only strings in the format of timestamp (10-digit).
 */
export interface DiagnosticResultModel {
  id: string;
  data_creator: string;
  data_updater: string;
  create_time: string;
  update_time: string;
  user_id: string;
  legal_area: string;
  response: string;
  total_score: number;
  urgency_level: DiagnosticResultUrgencyLevel;
  ai_report?: string | null;
}

/**
 * ORM class for DiagnosticResult entity.
 * It uses Singleton pattern to prevent re-initialization.
 */
export class DiagnosticResultORM {
  private static instance: DiagnosticResultORM | null = null;
  protected client: DataStoreClient;
  protected namespace: string;
  protected entityId: string;
  protected entityName: string;
  protected entityVersion: string;
  protected taskId: string;
  protected taskRootId: string;

  private constructor() {
    this.client = DataStoreClient.getInstance();
    this.namespace = '01987547fc6c72ecb453bd2736bd4ea0';
    this.entityId = '102019a692c38da79208e71c127a9db9e84';
    this.entityName = 'diagnostic_result';
    this.entityVersion = '102019a6e88bed67236939f705e5b6af790';
    this.taskId = '69120bf9228d86cc3a65654e';
    this.taskRootId = '6910a741b1b70bd284b566f2';
  }

  /**
   * Get singleton instance of DiagnosticResultORM
   */
  public static getInstance(): DiagnosticResultORM {
    if (!DiagnosticResultORM.instance) {
      DiagnosticResultORM.instance = new DiagnosticResultORM();
    }
    
    return DiagnosticResultORM.instance;
  }

  /**
   * Get all DiagnosticResult records
   * This returns all data without pagination.
   * To query data with page and filter conditions, use `list*()` instead.
   */
  async getAllDiagnosticResult(): Promise<DiagnosticResultModel[]> {
    const response = await this.client.all({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      format: {
        structured: true
      }
    });
    return this.resultToData(response.data?.values || []);
  }

  /**
   * Insert (create) new DiagnosticResult record(s)
   * DO NOT SET `id`, `data_creator`, `data_updater`, `create_time` and `update_time` since backend will fill it automatically.
   * It will respond the inserted record(s), with `id`, `data_creator`, `data_updater`, `create_time` and `update_time` filled by backend.
   */
  async insertDiagnosticResult(data: DiagnosticResultModel[]): Promise<DiagnosticResultModel[]> {
    const structured = data.map((item) => CreateData(DiagnosticResultModelToValues(item)));
    const response = await this.client.insert({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      batch: structured,
      format: {
        structured: true
      }
    });
    return this.resultToData(response.data?.values || []);
  }

  /**
   * Purge all DiagnosticResult records
   */
  async purgeAllDiagnosticResult(): Promise<void> {
    await this.client.purge({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      format: {
        structured: true
      }
    });
  }

  /**
   * List DiagnosticResult records with filters
   * This function provides search capabilities like filtering, sorting, pagination.
   */
  async listDiagnosticResult(filter?: Filter, sort?: Sort, paginate?: Page): Promise<[DiagnosticResultModel[], Page]> {
    const response = await this.client.list({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      filter: filter,
      sort: sort,
      paginate: paginate,
      format: {
        structured: true
      }
    });
    return [this.resultToData(response.data?.values || []), response.data?.page || { number: 0, size: 0 }];
  }


  /**
   * Get diagnostic_result by Id index
   * This function gets data by index.
   */
  async getDiagnosticResultById(
    id: string,
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexId(
      id
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) diagnostic_result by Id index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setDiagnosticResultById(
    id: string,
    data: DiagnosticResultModel
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexId(
      id
    );

    const values = DiagnosticResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete diagnostic_result by Id index
   */
  async deleteDiagnosticResultById(
    id: string
  ): Promise<void> {
    const index = createIndexId(
      id
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get diagnostic_result by DataCreator index
   * This function gets data by index.
   */
  async getDiagnosticResultByDataCreator(
    data_creator: string,
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexDataCreator(
      data_creator
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) diagnostic_result by DataCreator index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setDiagnosticResultByDataCreator(
    data_creator: string,
    data: DiagnosticResultModel
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexDataCreator(
      data_creator
    );

    const values = DiagnosticResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete diagnostic_result by DataCreator index
   */
  async deleteDiagnosticResultByDataCreator(
    data_creator: string
  ): Promise<void> {
    const index = createIndexDataCreator(
      data_creator
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get diagnostic_result by DataUpdater index
   * This function gets data by index.
   */
  async getDiagnosticResultByDataUpdater(
    data_updater: string,
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexDataUpdater(
      data_updater
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) diagnostic_result by DataUpdater index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setDiagnosticResultByDataUpdater(
    data_updater: string,
    data: DiagnosticResultModel
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexDataUpdater(
      data_updater
    );

    const values = DiagnosticResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete diagnostic_result by DataUpdater index
   */
  async deleteDiagnosticResultByDataUpdater(
    data_updater: string
  ): Promise<void> {
    const index = createIndexDataUpdater(
      data_updater
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }
  /**
   * Get diagnostic_result by UserId index
   * This function gets data by index.
   */
  async getDiagnosticResultByUserId(
    user_id: string,
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexUserId(
      user_id
    );

    const response = await this.client.get({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Set (update) diagnostic_result by UserId index
   * This function replaces data, so the data must be complete.
   * Must keep `id`, `data_creator`, `create_time` unchanged as original data (as fetched).
   * DO NOT SET `data_updater` and `update_time` since backend will fill it automatically.
   * It will respond the set record, with `data_updater` and `update_time` filled by backend.
   */
  async setDiagnosticResultByUserId(
    user_id: string,
    data: DiagnosticResultModel
  ): Promise<DiagnosticResultModel[]> {
    const index = createIndexUserId(
      user_id
    );

    const values = DiagnosticResultModelToValues(data);
    const structuredData = CreateData(values);

    const response = await this.client.set({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      data: structuredData,
      format: {
        structured: true
      }
    });

    return this.resultToData(response.data?.values || []);
  }

  /**
   * Delete diagnostic_result by UserId index
   */
  async deleteDiagnosticResultByUserId(
    user_id: string
  ): Promise<void> {
    const index = createIndexUserId(
      user_id
    );

    await this.client.delete({
      id: this.entityId,
      namespace: this.namespace,
      name: this.entityName,
      version: this.entityVersion,
      task: this.taskId,
      index: index,
      format: {
        structured: true
      }
    });
  }

  /**
   * Convert result data to DiagnosticResultModel data array
   */
  private resultToData(values: Data[]): DiagnosticResultModel[] {
    return values.map((item: Data) => {
      if (item.structured && item.structured.length > 0) {
        return DiagnosticResultModelFromValues(item.structured);
      }

      if (item.serialized) {
        try {
          const parsedData = JSON.parse(item.serialized) as DiagnosticResultModel;
          return parsedData;
        } catch (error) {
          console.error('Error parsing serialized DiagnosticResultModel data: ', error, item.serialized);
          return null;
        }
      }

      return null;
    }).filter((item): item is DiagnosticResultModel => item !== null);
  }
}

/**
 * Convert DiagnosticResultModel data to Value array
 */
function DiagnosticResultModelToValues(data: DiagnosticResultModel): Value[] {
  const fieldMappings = [
    { key: 'id', type: DataType.string, defaultValue: '' },
    { key: 'data_creator', type: DataType.string, defaultValue: '' },
    { key: 'data_updater', type: DataType.string, defaultValue: '' },
    { key: 'create_time', type: DataType.string, defaultValue: '' },
    { key: 'update_time', type: DataType.string, defaultValue: '' },
    { key: 'user_id', type: DataType.string, defaultValue: '' },
    { key: 'legal_area', type: DataType.string, defaultValue: '' },
    { key: 'response', type: DataType.string, defaultValue: '' },
    { key: 'total_score', type: DataType.number, defaultValue: 0 },
    { key: 'urgency_level', type: DataType.enumeration, defaultValue: 0 },
    { key: 'ai_report', type: DataType.string, defaultValue: null },
  ];

  return fieldMappings.map(({ key, type, defaultValue }) => {
    const value = data[key as keyof DiagnosticResultModel] ?? defaultValue;
    return CreateValue(type, value, key);
  });
}

/**
 * Convert Value array to DiagnosticResultModel data
 */
function DiagnosticResultModelFromValues(values: Value[]): DiagnosticResultModel {
  const data: Partial<DiagnosticResultModel> = {};
  
  for (const value of values) {
    if (!value.name) continue;
    
    switch (value.name) {
      case 'id':
        data.id = ParseValue(value, DataType.string) as string;
        break;
      case 'data_creator':
        data.data_creator = ParseValue(value, DataType.string) as string;
        break;
      case 'data_updater':
        data.data_updater = ParseValue(value, DataType.string) as string;
        break;
      case 'create_time':
        data.create_time = ParseValue(value, DataType.string) as string;
        break;
      case 'update_time':
        data.update_time = ParseValue(value, DataType.string) as string;
        break;
      case 'user_id':
        data.user_id = ParseValue(value, DataType.string) as string;
        break;
      case 'legal_area':
        data.legal_area = ParseValue(value, DataType.string) as string;
        break;
      case 'response':
        data.response = ParseValue(value, DataType.string) as string;
        break;
      case 'total_score':
        data.total_score = ParseValue(value, DataType.number) as number;
        break;
      case 'urgency_level':
        data.urgency_level = ParseValue(value, DataType.enumeration) as DiagnosticResultUrgencyLevel;
        break;
      case 'ai_report':
        data.ai_report = ParseValue(value, DataType.string) as string | null;
        break;
    }
  }
  
  return data as DiagnosticResultModel;
}


/**
 * Create index for Id fields
 */
function createIndexId(
  id: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, id, 'id'),
  ];

  return {
    fields: ['id'],
    values
  };
}
/**
 * Create index for DataCreator fields
 */
function createIndexDataCreator(
  data_creator: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, data_creator, 'data_creator'),
  ];

  return {
    fields: ['data_creator'],
    values
  };
}
/**
 * Create index for DataUpdater fields
 */
function createIndexDataUpdater(
  data_updater: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, data_updater, 'data_updater'),
  ];

  return {
    fields: ['data_updater'],
    values
  };
}
/**
 * Create index for UserId fields
 */
function createIndexUserId(
  user_id: string
): Index {
  const values: Value[] = [
    CreateValue(DataType.string, user_id, 'user_id'),
  ];

  return {
    fields: ['user_id'],
    values
  };
}

export default DiagnosticResultORM;