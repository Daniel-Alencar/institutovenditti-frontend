╔═══════════════════════════════════════════════════════════════════════╗
║           📋 COMANDOS PRONTOS - COPIAR E COLAR                        ║
╚═══════════════════════════════════════════════════════════════════════╝

Escolha o método e copie os comandos prontos!


┌───────────────────────────────────────────────────────────────────────┐
│  🚀 MÉTODO 1: DEPLOY VERCEL (MAIS RÁPIDO - 2 COMANDOS)               │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE no terminal:

  npm install -g vercel && vercel

  Depois:

  vercel --prod

  ✅ PRONTO! URL gerada automaticamente!


┌───────────────────────────────────────────────────────────────────────┐
│  🌐 MÉTODO 2: DEPLOY NETLIFY (3 COMANDOS)                            │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  npm install -g netlify-cli
  npm run build
  netlify deploy --prod

  ✅ URL gerada!


┌───────────────────────────────────────────────────────────────────────┐
│  📦 MÉTODO 3: EXPORTAR PARA ZIP (LINUX/MAC)                          │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  ./export-projeto.sh

  Ou manualmente:

  zip -r projeto-$(date +%Y%m%d).zip . -x "node_modules/*" -x "dist/*" -x ".git/*"


┌───────────────────────────────────────────────────────────────────────┐
│  📦 MÉTODO 4: EXPORTAR PARA ZIP (WINDOWS - PowerShell)               │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE no PowerShell:

  Compress-Archive -Path * -DestinationPath projeto.zip -Exclude node_modules,dist,.git


┌───────────────────────────────────────────────────────────────────────┐
│  🔄 CONFIGURAR NO NOVO LOCAL (4 COMANDOS)                             │
└───────────────────────────────────────────────────────────────────────┘

  Após extrair/copiar arquivos, COPIE E COLE:

  npm install
  cp .env.example .env.local
  npm run dev

  Se quiser validar antes:

  npm run check:safe


┌───────────────────────────────────────────────────────────────────────┐
│  🏗️ BUILD PARA PRODUÇÃO (2 COMANDOS)                                 │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  npm run build
  npm run preview

  Para testar o build localmente antes de publicar


┌───────────────────────────────────────────────────────────────────────┐
│  📤 GIT + GITHUB (COPIAR PARA REPOSITÓRIO)                            │
└───────────────────────────────────────────────────────────────────────┘

  1. Criar repositório no GitHub primeiro!

  2. COPIE E COLE (substitua URL_DO_SEU_REPO):

  git init
  git add .
  git commit -m "Initial commit - Advocacia Digital"
  git remote add origin URL_DO_SEU_REPO
  git push -u origin main


┌───────────────────────────────────────────────────────────────────────┐
│  🔐 CONFIGURAR SENHA ADMIN                                            │
└───────────────────────────────────────────────────────────────────────┘

  LINUX/MAC - COPIE E COLE (substitua SENHA):

  echo 'VITE_ADMIN_PASSWORD=SUA_SENHA_AQUI' > .env.local

  WINDOWS - COPIE E COLE no PowerShell:

  'VITE_ADMIN_PASSWORD=SUA_SENHA_AQUI' | Out-File -FilePath .env.local -Encoding utf8


┌───────────────────────────────────────────────────────────────────────┐
│  🧹 LIMPAR E REINSTALAR (SE DER ERRO)                                 │
└───────────────────────────────────────────────────────────────────────┘

  LINUX/MAC - COPIE E COLE:

  rm -rf node_modules package-lock.json dist && npm install

  WINDOWS - COPIE E COLE no PowerShell:

  Remove-Item -Recurse -Force node_modules,package-lock.json,dist; npm install


┌───────────────────────────────────────────────────────────────────────┐
│  📊 VERIFICAR SE TUDO ESTÁ OK                                         │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  npm run check:safe && npm run build

  Se passar sem erros = ✅ PRONTO!


┌───────────────────────────────────────────────────────────────────────┐
│  🚀 DEPLOY COMPLETO VERCEL (TUDO DE UMA VEZ)                         │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE (automatiza tudo):

  npm install -g vercel && npm run build && vercel --prod

  Vai instalar CLI, fazer build e publicar!


┌───────────────────────────────────────────────────────────────────────┐
│  🚀 DEPLOY COMPLETO NETLIFY (TUDO DE UMA VEZ)                        │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  npm install -g netlify-cli && npm run build && netlify deploy --prod


┌───────────────────────────────────────────────────────────────────────┐
│  🔍 TESTAR LOCALMENTE (COMPLETO)                                      │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE (faz tudo: instala, configura, testa):

  npm install && cp .env.example .env.local && npm run dev


┌───────────────────────────────────────────────────────────────────────┐
│  📱 TESTAR ADMIN (APÓS npm run dev)                                   │
└───────────────────────────────────────────────────────────────────────┘

  URL para testar admin localmente:

  http://localhost:3000/?admin=true

  Senha padrão (se usar .env.example):

  admin123


┌───────────────────────────────────────────────────────────────────────┐
│  🌍 URLs DE HOSPEDAGEM GRATUITA                                       │
└───────────────────────────────────────────────────────────────────────┘

  Vercel:     https://vercel.com
  Netlify:    https://netlify.com
  GitHub:     https://github.com
  Render:     https://render.com
  Railway:    https://railway.app


┌───────────────────────────────────────────────────────────────────────┐
│  📋 CHECKLIST RÁPIDO                                                  │
└───────────────────────────────────────────────────────────────────────┘

  Cole este comando para verificar arquivos essenciais:

  LINUX/MAC:
  ls -1 package.json vite.config.ts index.html src/.gitignore 2>/dev/null | wc -l

  Deve retornar: 5 (se todos os arquivos existem)

  WINDOWS PowerShell:
  @('package.json','vite.config.ts','index.html','src','.gitignore') | ForEach-Object { Test-Path $_ }

  Deve retornar: True True True True True


┌───────────────────────────────────────────────────────────────────────┐
│  🔄 ATUALIZAR DEPLOY EXISTENTE                                        │
└───────────────────────────────────────────────────────────────────────┘

  VERCEL - COPIE E COLE:

  npm run build && vercel --prod

  NETLIFY - COPIE E COLE:

  npm run build && netlify deploy --prod

  GIT (se configurou auto-deploy) - COPIE E COLE:

  git add . && git commit -m "Update" && git push


┌───────────────────────────────────────────────────────────────────────┐
│  📖 VER VERSÕES INSTALADAS                                            │
└───────────────────────────────────────────────────────────────────────┘

  COPIE E COLE:

  node -v && npm -v && npx vite --version


┌───────────────────────────────────────────────────────────────────────┐
│  🆘 COMANDOS DE EMERGÊNCIA                                            │
└───────────────────────────────────────────────────────────────────────┘

  Se NADA funcionar, COPIE E COLE (reset completo):

  LINUX/MAC:
  rm -rf node_modules package-lock.json dist .env.local && \
  cp .env.example .env.local && \
  npm install && \
  npm run build && \
  npm run dev

  WINDOWS PowerShell:
  Remove-Item -Recurse -Force node_modules,package-lock.json,dist,.env.local; Copy-Item .env.example .env.local; npm install; npm run build; npm run dev


═══════════════════════════════════════════════════════════════════════

  💡 DICA: Salve este arquivo! Copie e cole conforme necessário.

  🎯 MAIS RÁPIDO:

  npm install -g vercel && vercel --prod

  ✅ 1 COMANDO = SITE NO AR!

═══════════════════════════════════════════════════════════════════════
